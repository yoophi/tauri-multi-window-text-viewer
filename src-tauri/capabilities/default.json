{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all windows (welcome + per-file viewers)",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
